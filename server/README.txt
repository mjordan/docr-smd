# docr/smd server

## Overview

## Setup

Make sure the SQLite database file is writable by both the user who runs the queue manager and the web server.

## Controlling access to your docr page server

Tokens, IP whitelisting
Be carefull with restricting access if you are a peer.
